function main { } BEGIN
  let x = 1 
  let y = 5

  while {x < y} BEGIN
    x = x + 3
    y = y + 2
    if {x < y} BEGIN
    call PRINT{ "x: ", x ," is < y: ", y }
    END
    else BEGIN
      call PRINT{ "x: ", x ," is > y: ", y }
    END
  END
  
END
call main{}